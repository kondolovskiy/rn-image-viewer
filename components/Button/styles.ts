import { StyleSheet } from "react-native"

export default StyleSheet.create({

  button: {
    width: '100%',
    backgroundColor: '#0b0',
    alignItems: 'center',
    paddingVertical: 10,
    marginTop: 5
  },

  buttonDisabled: {
    backgroundColor: '#000',
  },

  buttonText: {
    color: '#fff',
    textTransform: 'uppercase'
  }
})