import { StyleSheet } from "react-native"

export default StyleSheet.create({
  wrapper: {
     alignItems: 'center' 
  },

  imageContainer: {
    height: 350, 
    alignItems: 'center', 
    justifyContent: 'center' 
  }
})