import { StyleSheet } from "react-native"

export default StyleSheet.create({
  wrapper: {
    width: '80%',
    alignSelf: 'center',
    marginTop: 150
  },

  headerText: {
    textAlign: 'center',
    marginBottom: 30
  }
})